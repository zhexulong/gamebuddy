/** User-visible paths that move out of the host when Rust authority is active. */
export const RUST_MODE_HOST_PATHS_LINE =
    "Host backends → MODULE: ctx_memory, ctx_note; historian: module-side";
