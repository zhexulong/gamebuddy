{
  "name": "gamebuddy",
  "version": "0.0.0",
  "private": true,
  "packageManager": "pnpm@11.1.3",
  "engines": {
    "node": ">=24.13.0 <25",
    "pnpm": ">=11.1.3 <12"
  },
  "scripts": {
    "build": "pnpm --filter @gamebuddy/companion-host build",
    "test": "pnpm --filter @gamebuddy/companion-host test",
    "typecheck": "pnpm --filter @gamebuddy/companion-host typecheck"
  }
}
