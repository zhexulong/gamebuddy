# GameBuddy Stardew integration

This directory contains the formal Stardew Attachment / Provisioning slice plus an earlier local split-screen Farmhand Embodiment and bridge fixture. The split-screen code is retained as historical development material and is **not** the formal product topology or Phase 1 acceptance path.

The formal topology is an independent Stardew AI client joining the human host through a version-locked, non-UI Farmhand Provisioning path. `HostFarmhandProvisioner` publishes a signed live advertisement, accepts only a signed request made after App confirmation, creates a native Farmhand through `Cabin.CreateFarmhand()`, persists the Companion/save/world/cabin/native-ID binding in save data, and issues a signed short-lived manifest. `StardewAttachmentFlow` is the App-owned discovery/confirmation/file exchange boundary; it has no Agent tool and never accepts endpoint or Farmhand ID from model text. The client `FarmhandProvisioner` validates the manifest, exact ID, target game/protocol/integration versions, expiry, signature and save/world scope before reusing the native LAN client. It reaches Embodiment only after `readyToPlay` and local `Game1.player` identity match. Neither side drives Stardew UI, injects OS input, constructs a fake `Farmer`, mutates `Game1.otherFarmers`, controls a remote player, exposes a public network listener, teleports the actor, or claims a shadow actor is a Farmhand.

The fixture below must not be used as evidence that the independent-client path is implemented.

The included `StardewBodyController` is a bounded game-thread movement executor, not an Agent or a player-facing Game Action surface. It accepts a structured target from `ExecutionManager`, maintains single movement ownership, atomically invalidates superseded directives before replacement, uses the target-version native `PathFindController` for obstacle-aware routing, records accepted/running/progress/blocked/terminal traces with route revision and local actor evidence, is cancellable locally, and stops on menu/event/movement-lock/deadline conditions. Furniture and other native collision are therefore routed around when a native path exists; they are not treated as a permanent fixture gate. Native Warp targets have a narrowly bounded adjacent-arrival rule because the Warp source tile may not be standable; ordinary movement still requires the exact target tile.

For Phase 1 native-mechanics evidence, `equip_tool`, `travel`, `enter_exit`, and `till_soil` are available through the formal AI-client named-pipe bridge under the versioned default-consent policy. `enter_exit` is restricted to live native door targets and completes only after the target-version `Warped` postcondition. `travel` is restricted to live native Warp facts and completes only after the target-version `Warped` postcondition. Its preparation move uses target-version `PathFindController` and accepts a bounded Warp-adjacent arrival when the Warp source tile is not standable. `till_soil` accepts only live diggable tiles and requires a native `HoeDirt` postcondition. Its dedicated `native_till_soil_v1` fixture gate passed after formal attachment: production equipped the live Hoe and rediscovered Farm target `(37,18)`, then target-version `Hoe.DoFunction` returned `succeeded/soil_tilled` with `before=none;after=HoeDirt`; the exact bare target disappeared from the next snapshot (`soilTiles 9→8`). `clear_debris`, `npc_relationship`, and `pet_animal` remain experimental native slices that require explicit `ExperimentalActions` in a local test profile and remain hidden from the Agent registry. `clear_debris` has passed one isolated target-version native-local mechanics gate (fixed `(62,17)` parent-`752` ResourceClump, eight Pickaxe hits, terminal `succeeded/debris_cleared`, fresh target absence, verified teardown); that shared mechanics evidence does not satisfy its independent Farmhand gate or authorize publication. The former `collect_resource` action is retired at every bridge surface: a Tree source transform and later Debris delivery are distinct lifecycles, so future gameplay support must compose a bounded source transform with fresh discovered `pickup_item` delivery evidence. `pickup_item` is published after its independent native Debris fixture gate: the bounded action approaches the same opaque target and waits for target-version magnetic collection, then requires `succeeded/item_picked_up`, exact chunk removal, Farmhand inventory `0→1`, and target disappearance; it never directly calls `Debris.collect`. `harvest_crop` is now published after its dedicated native fixture gate and complete promotion regression. `pickup_forage` is now published after its dedicated native fixture gate: production discovered opaque target `forage_d4ef0057da329ade` at Farm `(25,34)` for `(O)399`, received `succeeded/forage_picked_up`, proved native object removal and Farmhand inventory `0→1`, and observed the target disappear from the next snapshot. `machine_inspect` is published after a dedicated read-only native fixture gate: a formal Farmhand discovered a native Keg, received `succeeded/machine_inspected`, and the receipt's machine/held-input/output-ready facts matched the next live snapshot for the same opaque target without opening a menu or changing machine state. `water_crop` is published after a dedicated native fixture gate: the fixture creates an unwatered growing crop without invoking debug `Water`, and the production adapter independently rediscovers it and calls target-version `WateringCan.DoFunction`. Success requires the exact `HoeDirt` to change from unwatered to watered and one Watering Can charge to be consumed. `pet_animal` is an experimental native slice with a completed static contract and runner but a blocked live gate in the current save: it only accepts a live unpetted Pet while the Farmhand has empty hands, calls native `Pet.checkAction`, and requires both the daily pet marker and native friendship callback as postconditions. The formal FarmHouse and Farm probes both found `petTargets=0`, so it remains hidden and unpublished. `collect_animal_product` is published after a dedicated native fixture success gate. It only publishes a nearby adult animal whose live `currentProduce`, compatible Farmhand-owned `MilkPail`/`Shears` slot, and inventory capacity all pass native checks. It maps the revalidated target tile to a cardinal tool direction, begins target-version Farmer-owned `BeginUsingTool`, and verifies the native tool bound the same animal ID before accepting the execution. Only the later native `Tool.DoFunction` may clear that animal's produce and add the expected produce stack; then it issues `succeeded/animal_product_collected`. It never calls generic `checkAction`, writes produce, or adds inventory directly. The fixture proof used a live Sheep and Shears, with `(O)440` inventory `0→1`, cleared produce, completed animation, and disappeared target; ordinary worlds without a qualifying target still report blocked. `plant_seed` is published after a dedicated native fixture gate. It accepts only a nearby empty ground `HoeDirt` paired with a Farmhand-owned ordinary Seed slot, invokes target-version `Object.placementAction → HoeDirt.plant`, and succeeds only when that exact tile gains a native crop, total matching seed inventory decreases by exactly one, and the opaque target disappears. `place_wood_fence` is separately restricted to the finite source slot `(O)322`; its target-version 1.6.15 boundary is the guarded `Object.placementAction` call whose `IsFenceItem` branch constructs `new Fence` at the target tile. The bridge never exposes generic item placement and no non-`(O)322` Object can reach this wrapper; completion requires the same-tile native non-gate Fence result, positive `health/maxHealth`, and one-item inventory decrement. It excludes Indoor Pots, tree/fruit seed branches, stale targets, and direct crop/inventory mutation. The fixture's attachment-before `native_plant_seed_v1` setup supplied season-valid `(O)479` Melon Seeds and used target-version `RemoveDirt`/`SpreadDirt` only to establish legal empty dirt; it never created a crop or receipt. Production proof from fresh Farm target `seed_80e63785ce46ffbc` at `(47,31)` returned `succeeded/seed_planted`, native crop `479`, seed inventory `2→1`, and target `9→8`.

`feed_animal` is published but deliberately narrower than its name suggests: it accepts only a nearby live empty `AnimalHouse` `Trough` and an owned `(O)178` Hay slot, invokes native `AnimalHouse.checkAction`, and succeeds only after the trough contains Hay and the Farmhand Hay total decreases by exactly one. It never reports an animal as fed/full; animal fullness belongs to later native day-update processing. The static native-local `native_feed_animal_v1` fixture is a separate single-process lane: before bridge attachment it may use target-version `SetupBigFarm` only to establish and verify a native AnimalHouse, empty Trough, resolvable entry fact, and current-local-player Hay. It never invokes `AnimalHouse.checkAction`, fills a trough, decrements Hay, emits a receipt, or uses Farmhand/HostAutomation evidence. Its dedicated runner separately receipts typed travel/movement/enter-exit, discovers opaque trough facts only from a fresh post-entry snapshot, blocks on zero or ambiguous targets, and requires feed's same request/execution `succeeded/hay_placed_in_trough`, Hay `N→N-1`, target disappearance, and a fresh actionable snapshot. No live native-local gate has been run for this new slice. `clear_debris` is restricted to live target-version `ResourceClump` facts and native Axe/Pickaxe upgrade preconditions; partial clump hits are not terminal success. Its fixed native-local fixture gate passed with final `succeeded/debris_cleared` plus fresh exact-target absence, but it remains experimental until independent Farmhand closure and promotion evidence exist. `machine_inspect` is published and read-only: it reports only live machine state and never opens a menu or changes the machine. `npc_relationship` is also read-only: it reports only an existing Farmhand friendship record for a nearby NPC and never calls `NPC.checkAction`, opens dialogue, creates a friendship record, or changes relationship state. The former `collect_resource` bridge action is retired, not experimental: target-version `Tree.performTreeFall` creates RESOURCE chunks without parent-stump/request identity and their later `Debris.collect` lifecycle is separate. A future player intent must compose independently verified bounded source-transform and fresh `pickup_item` delivery steps; no retired one-step action or debris count may report `succeeded/resource_collected`. `harvest_crop` is a published native action restricted to nearby ready ordinary `Grab` crops; the Mod rejects the Golden Scythe override, then invokes target-version `HoeDirt.performUseAction`, whose native path dispatches `Crop.harvest` and applies crop destruction only where appropriate. Its dedicated fixture gate used native `SetupBigFarm`, `SpreadSeeds 480`, and `GrowCrops 11` to establish an in-season Tomato; production obtained `succeeded/crop_harvested` at Farm `(39,19)` for crop `480`, inventory `(O)256` `0→1`, and native regrow evidence (`phase 5`, `dayOfPhase 0→4`) while the target stopped being ready. Completion requires native inventory gain plus regrow/removal postconditions; the regrow path intentionally retains the crop and its wrapper return is not treated as a failed receipt. Scythe and forage crops are excluded, and a missing ready crop is a blocked world-state result. `use_item` is a published native action restricted to ordinary edible inventory `Object` slots; the Mod invokes `Farmer.eatHeldObject` on the live slot and waits for native animation completion plus a one-item stack reduction. It does not open a confirmation menu, call arbitrary item actions, or write stamina/health/inventory directly. Published actions are enabled by default; the player may hide an action or action family with `DeniedActions` or `DeniedActionFamilies`. It selects a Tool already owned by this Farmhand, does not consume an item, stamina, time, or world resource, and returns authoritative before/expected/after `CurrentTool` evidence. The verified live smoke uses `tools/run-stardew-equip-tool-smoke.mjs`; it is a single capability proof, not evidence that movement, resource-changing actions, multi-step execution, or Agent planning are complete. The older `gamebuddy_equip_tool_fixture <inventory-slot> <request-id>` console command remains a local diagnostic fixture only.

`BridgeProtocol` and `BridgeSession` define a bounded, versioned, authenticated **game-thread-only** protocol façade. `LocalPipeBridge` is the selected opt-in Windows local transport candidate: it is a current-user-only named pipe, frames bounded UTF-8 JSON on background tasks, attaches every queue item to a connection generation, and lets `UpdateTicked` drain at most eight requests. It never calls Stardew APIs off the game thread. Authentication opens transport only; scope, player-enabled capability, deadline, idempotency, cancellation IDs, request shape, and game-state preconditions are revalidated by the Mod before an execution can reach `ExecutionManager`.

### Player action policy

`config.json` is the local, player-controlled source of Game Action authorization. With `ActionPolicyVersion: 1`, every published and verified action is consented by default. The player only declares exceptions:

```json
{
  "ActionPolicyVersion": 1,
  "DeniedActions": ["equip_tool"],
  "DeniedActionFamilies": [],
  "ExperimentalActions": []
}
```

Denied actions and families are removed from the Mod capability advertisement and therefore are not disclosed to the Agent through tools, catalog, search, snapshots, or knowledge projections. `equip_tool` only selects a Tool already owned in the AI Farmhand's inventory. The Mod returns authoritative before/expected/after `CurrentTool` evidence and does not consume a resource.

The Host only mounts the corresponding tool while that capability remains declared; it never mints a token, interprets model text as permission, or enables an action itself. The Mod uses a generic `action_not_available` response for stale tools after capability withdrawal. Local cancellation remains available regardless of the Game Action policy. Profiles that still use legacy `EnabledActions` retain the old fail-closed allowlist semantics until explicitly migrated.

Enable the bridge only in the local untracked `config.json` installed with the AI-client Integration Mod; `config.example.json` documents the shape. `PlayerId` is the native independent-client Farmhand's `UniqueMultiplayerID`, not the human host's ID. It needs opaque IDs and a 16+ character token; it never listens on LAN or the public network. The formal Windows AI-Farmhand bridge has now passed live move, replacement/cancel, disconnect invalidation, same-request reconnect replay, native Warp travel, and native soil tilling. The Wave 1 travel proof used the live FarmHouse Warp `(3,12) -> Farm:25,33`; target-version PathFindController routed the Farmhand from `(9,9)` around furniture to `(3,11)`, the bounded Warp-adjacent receipt completed, and the native `Warped` receipt verified `Farm:25,33`. The tilling proof then discovered live `soilTiles` on Farm, selected `(24,34)`, and verified `succeeded/soil_tilled` with a real `HoeDirt` postcondition. `clear_debris` remains experimental because its independent Farmhand live gate has not passed; its native-local receipt/postcondition result is mechanics evidence only. Published `pickup_item` uses the dedicated `native_pickup_item_v1` fixture, which creates a target-version OBJECT Debris drop and may pin its fixture-only dropped-by identity only as a short handoff guard. Target-version `Debris.updateChunks` begins magnetic pickup after roughly 600 ms and owns `Debris.collect`, so the action never uses a fake click-style collect call: it drives only the Farmhand's native approach to the same opaque chunk, then requires native magnetic delivery, exact chunk disappearance, and exact inventory gain. Its formal receipt was `succeeded/item_picked_up` for Farm `(21,29)` `(O)388`, with Farmhand inventory `0→1` and the target absent from the next snapshot. Published `pickup_forage` binds a live opaque target identity to its location, tile, qualified item, stack, and native forage-object fact; it invokes target-version `Game1.tryToCheckAt` on the game thread, preserving the ordinary-player bridge/radius/hook ingress before native `GameLocation.checkAction`, and reports success only when the same object is removed and the Farmhand inventory increases. The normal Agent registry now exposes this bounded action under the default-consent policy. `fertilize_tile` is published after its dedicated native fixture gate. It is limited to a nearby ground `HoeDirt` that target-version `CanApplyFertilizer` accepts, paired with the Farmhand's exact fertilizer slot. It invokes that item's native `placementAction` and reports success only when the exact native fertilizer value is set and total inventory decreases by one; indoor pots and direct terrain/inventory mutations are excluded. The formal fixture proof obtained `succeeded/fertilizer_applied` at Farm `(38,28)`, `(O)368` from none, inventory `2→1`, and target disappearance in the fresh snapshot. `water_crop` has passed a dedicated fixture gate: production `WateringCan.DoFunction` changed native Farm `HoeDirt` `(38,18)` from unwatered to watered, consumed water `40→39`, returned `succeeded/crop_watered`, and removed the target from the next live snapshot. A missing live target for those actions is a world-state precondition result, not a claim that obstacle-aware movement or the already-published Wave 1 actions are blocked. This is evidence for the bounded published actions, not a claim of complete production IPC, all action policies, or full Phase 3/4 acceptance.

### Retired split-screen UI fixture

The former one-process `PerScreen<T>` split-screen fixture predates the formal independent-client topology. It is not a supported product attachment flow, cannot serve as evidence for a native AI Farmhand, and must not be reused as the formal AI-client state model. The UI/XInput automation scripts that previously assisted that fixture were removed: the formal Integration uses per-client state and native Farmhand identity after non-UI provisioning.

## Local compilation

The official SMAPI build configuration compiles against the game's proprietary assemblies. A developer with a local Stardew Valley + SMAPI installation can provide its path outside Git using a `stardewvalley.targets` file in their user profile:

```xml
<Project>
  <PropertyGroup>
    <GamePath>C:\path\to\Stardew Valley</GamePath>
  </PropertyGroup>
</Project>
```

Then run:

```powershell
dotnet build GameBuddy.sln --configuration Release
```

Do not commit a game installation path, game DLLs, SMAPI binaries, generated mod output, Steam credentials, invitation codes, account IDs, or save files. CI restores the locked NuGet package and validates the fail-closed local-embodiment source shape; it cannot compile against proprietary game assemblies without a licensed installation.

## Formal Attachment Flow

Configure separate host and AI-client Mod profiles with the same local session directory and session token, and the locked `1.6.15` game version. The Host generates a fresh session nonce on every process start; the App and AI client must consume that current signed advertisement rather than reuse a configured nonce. The host profile must list the authorized opaque Companion ID. With a real host save loaded and the host LAN server ready, the App reads and verifies `stardew-session.json`, displays the host scope and cabin facts, and asks the player to confirm the selected Companion/cabin. Only after confirmation it writes a signed `stardew-attachment-request.json`; the Host Mod consumes it on the game thread, creates or reuses the exact native Farmhand binding, persists the binding through the game's save data, and writes `stardew-farmhand-manifest.json`. Copying or editing a manifest, endpoint, Farmhand ID, scope, or action policy is rejected.

The AI-client Mod reads that manifest from its local profile and starts the native LAN adapter. Its bridge remains closed until `readyToPlay`, exact `Game1.player.UniqueMultiplayerID`, manifest save/world scope, and target-version checks all pass. The diagnostic `FarmhandProvisioningProbe` remains available only as a separately opt-in version-locked regression fixture. The local target-version regression has passed the non-UI attachment/reconnect/Host-restart hard scenarios, and the formal named-pipe bridge has passed one `equip_tool` action with native before/expected/after evidence. This does not cover day transition, resource-changing actions, movement, multi-step execution, Agent planning, or complete release BDD. Use `tools/check-stardew-phase1-prerequisites.ps1 -GamePath <path> -AiClientModConfigPath <path> -SessionDirectory <absolute-path> -ExpectedFarmhandId <native-id>` to report the environment gate.

## Legacy fixture gate (not formal attachment)

The old local split-screen fixture requires one legitimate Stardew process and a second local input device. Start the human save, use Stardew's official `Start Local Co-op` flow for the second local player, then run `gamebuddy_farmhands` in the SMAPI console. Copy the intended native Farmhand's reported `player_id` into the local untracked `config.json` `PlayerId` field. Reload the shared save so GameBuddy can bind that screen, then run the following only from the configured AI Farmhand screen's SMAPI console:

```text
gamebuddy_farmhands
gamebuddy_status
gamebuddy_move_fixture <nearby-x> <nearby-y> phase1_move_01
# Before the first route reaches its target, replace it with a new directive:
gamebuddy_move_fixture <other-nearby-x> <other-nearby-y> phase1_move_02
gamebuddy_equip_tool_fixture <tool-slot> phase1_equip_01
gamebuddy_cancel
gamebuddy_trace
```

Capture the log showing `bound configured AI Farmhand only`, the Farmhand multiplayer ID and `screen_id`, plus the JSON emitted by `gamebuddy_trace`. Verify that replacement records a terminal `superseded_by_new_directive` receipt before the new request's distinct route revision, then compare the trace and native tool before/after receipt to the matching human-screen-visible result. Exercise replacement, bounded blockage recovery, menu/warp invalidation, saving/cutover, local-co-op reconnect, and the native tool fixture in the dedicated test save. This manual evidence is required before declaring Phase 1 accepted; a single-screen compile/smoke, a shadow `Farmer`, or a bridge fixture is not a substitute.
