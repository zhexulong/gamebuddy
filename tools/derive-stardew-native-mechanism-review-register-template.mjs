import { readFile, rename, writeFile } from "node:fs/promises";
import path from "node:path";
import { validateMechanismReport } from "./lib/stardew-native-mechanism-review-register.mjs";

function fail(code, message, details = {}) { const error = new Error(message); error.code = code; error.details = details; throw error; }
function parseArgs(argv) { const result = {}; for (let index = 0; index < argv.length; index += 1) { const option = argv[index]; const value = argv[++index]; if (!option?.startsWith("--") || !value || value.startsWith("--")) fail("mechanism_review_template_arguments_invalid", "Usage: --mechanism-report <exact-report.json> --out <template.json>"); result[option.slice(2)] = value; } if (!result["mechanism-report"] || !result.out) fail("mechanism_review_template_arguments_required", "Usage: --mechanism-report <exact-report.json> --out <template.json>"); return result; }
async function readJson(filePath, label) { try { return JSON.parse(await readFile(filePath, "utf8")); } catch (error) { fail("mechanism_review_template_json_invalid", `Could not read ${label} JSON.`, { filePath, cause: error.message }); } }
async function atomicWrite(filePath, value) { const output = path.resolve(filePath); const temporary = `${output}.${process.pid}.${Date.now()}.tmp`; await writeFile(temporary, `${JSON.stringify(value, null, 2)}\n`); await rename(temporary, output); }
async function main() { const args = parseArgs(process.argv.slice(2).filter((value) => value !== "--")); const report = await readJson(args["mechanism-report"], "mechanism report"); const exact = validateMechanismReport(report); const records = [...exact.mechanisms.values()].map((mechanism) => ({ mechanismId: mechanism.mechanismId, disposition: "unresolved_gap", reason: "No source-owner/handoff review has yet disposed this exact syntax discovery row; it remains a blocking review gap.", possiblyGameplayBearing: true })); const template = { schemaVersion: 1, artifactKind: "native_interaction_mechanism_review_register", attestation: { targetAssemblySha256: exact.targetAssemblySha256, sourceManifestSha256: exact.sourceManifestSha256 }, records }; await atomicWrite(args.out, template); process.stdout.write(`${JSON.stringify({ artifactKind: "native_interaction_mechanism_review_register_template", mechanismCount: records.length, initialBlockingGapCount: records.length, disposition: "unresolved_gap" })}\n`); }
main().catch((error) => { process.stderr.write(`${error.code ?? "mechanism_review_template_failed"}: ${error.message}\n`); process.exitCode = 1; });
