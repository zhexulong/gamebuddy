/**
 * Production provisioning is intentionally not a general host API. Canonical
 * deployment composition owns authority creation; raw store/provision handles
 * remain in the internal implementation for that closed construction path.
 */
export {};
